"""
[Title/Звание]

RastPy — Софтуерен растеризатор в Python

[Description/Обрисовка]

RastPy е софтуерен 3D растеризатор, написан изцяло на Python, който зарежда
3D модели от .obj файлове и ги рендерира като 2D изображения. Проектът
имплементира целия класически растеризационен pipeline — от трансформации
на върховете до z-буфер и shading — без да използва GPU или OpenGL,
разчитайки единствено на NumPy за векторните операции и Pillow за изходния файл.

[Functionalities/Надарености]

Потребителят зарежда .obj файл чрез аргумент на командния ред или конфигурационен файл.
Програмата парсва .obj файла и извлича върховете и триъгълниците на модела.
Потребителят задава позиция на камерата, посока на гледане и ъгъл на видимост (FOV).
Потребителят задава позиция и цвят на точков източник на светлина.
Програмата прилага Model/View/Projection матрични трансформации върху всеки връх чрез NumPy.
Програмата растеризира всеки триъгълник — определя кои пиксели попадат в него
чрез барицентрични координати.
Програмата поддържа z-буфер за коректно наслагване на припокриващи се обекти.
Програмата изчислява flat shading на база нормалата на всеки триъгълник и посоката на светлината.
(Разширение - ако сесията не ме приключи) Програмата поддържа Gouraud shading чрез интерполация на нормалите по върховете.
Готовото изображение се записва като PNG файл чрез Pillow.

[Milestones/Възлови точки]

1. .obj парсър — зареждане на върхове (v) и лица (f) от файл.
2. Математически модул — матрици за трансформация (translation, rotation, scale),
   перспективна проекция, барицентрични координати, нормали на триъгълници.
3. Растеризатор — за всеки триъгълник определя обхващащия правоъгълник (bounding box),
   итерира пикселите и проверява принадлежност.
4. Z-буфер — структура, която пази най-близката дълбочина за всеки пиксел
   и отхвърля скритите фрагменти.
5. Shading модул — flat shading (един цвят на триъгълник);
   (разширение) Gouraud shading с интерполация.
6. Изходен модул — запис на финалния пиксел-буфер като PNG чрез Pillow.
7. Конфигурационен интерфейс — четене на параметри (камера, светлина, входен файл)
   от командния ред или JSON конфигурационен файл.

[Estimate in man-hours/Времеоценка в човекочасове]

~45 часа

Разбивка по milestones:
- да разбера как работи rasterization: 5-6ч
- .obj парсър: 3ч
- Математически модул: 8ч
- Растеризатор + z-буфер: 10ч
- Shading модул: 6ч
- Изходен модул: 2ч
- Конфигурационен интерфейс: 3ч
- Тестове: 5ч

[Usage of technologies/Потребление на технологии]

- numpy — матрични операции, векторна математика, пиксел-буфер
- Pillow (PIL) — запис на изходното изображение като PNG
- pytest — unit тестове
- библиотека за парсване на JSON 
"""